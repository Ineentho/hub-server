machine:
  services:
    - docker

dependencies:
  override:
    - pip install docker-compose

test:
  override:
    - docker-compose run main ./runtests
  post:
    - mv coverage.xml $CIRCLE_TEST_REPORTS